import ProductTour from '@/components/deck/ProductTour';

export const metadata = {
  title: 'DigiHome — Interaktiv omvisning',
  robots: { index: false, follow: false },
};

export default function OmvisningPage() {
  return <ProductTour />;
}
